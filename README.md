
  # Exact Screen Replication

  This is a code bundle for Exact Screen Replication. The original project is available at https://www.figma.com/design/md5DPG01oH9j3LNHapbhIo/Exact-Screen-Replication.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  